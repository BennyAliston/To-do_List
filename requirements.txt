# Core dependencies
tkcalendar>=1.6.1    # Calendar widget for date selection
pillow>=9.0.0        # Required for some tkinter image handling

# Note: tkinter ships with Python — no pip install needed